name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

env:
  HOMEBREW_NO_AUTO_UPDATE: 1

jobs:
  build-and-test:
    name: Build & Test (Swift)
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # pinned SHA
        with:
          persist-credentials: false

      - name: Show environment info
        run: |
          sw_vers
          uname -m
          xcode-select -p
          xcodebuild -version
          swift --version

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Create artifacts directory
        run: mkdir -p artifacts

      - name: Generate Xcode project
        run: cd src && xcodegen generate

      - name: Build for testing
        run: |
          cd src
          xcodebuild build-for-testing \
            -project Clabotch.xcodeproj \
            -scheme Clabotch \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath ../artifacts/DerivedData \
            -resultBundlePath ../artifacts/build.xcresult \
            CODE_SIGNING_ALLOWED=NO

      - name: Test without rebuilding
        run: |
          cd src
          xcodebuild test-without-building \
            -project Clabotch.xcodeproj \
            -scheme Clabotch \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath ../artifacts/DerivedData \
            -resultBundlePath ../artifacts/test.xcresult

      - name: Run hook script tests
        run: bash tests/test_hooks.sh

      - name: Upload xcresult artifacts
        if: ${{ failure() }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # pinned SHA
        with:
          name: xcresult-${{ github.run_id }}
          path: artifacts/*.xcresult
          retention-days: 7
